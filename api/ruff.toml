# Mirrors api/pyproject.toml [tool.ruff] so editors without PEP 621 support still pick it up.
line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "RUF", "ASYNC"]
ignore = ["E501"]

[lint.per-file-ignores]
"tests/*" = ["S101"]
